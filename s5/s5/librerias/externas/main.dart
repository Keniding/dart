void main() {
  // Similar a npm de node, gestor de librerias y dependencias
  // En dart existe pub
  // Creamos un nuevo proyecto con un nuevo proyecto: (para tener el archivo de configuracion)
  // El cual no permitira importar librerias externas
  // => dart create -t console example

  // Y se ejecuta con: 
  // => dart run example.dart
  //  cd example
  // dart run

  // Asi mismo para instalar las dependencias usa
  // => dart pub get
}